version: '3.8'
services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - ./redis-/data
    command: redis-server --appendonly yes

  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    depends_on:
      - redis
